# 🧠 AI SYSTEM INSTRUCTIONS FOR SENTIRIC-OBSERVER

Bu repoda kod yazan her AI ajanı aşağıdaki Rust/Sovereign kurallarına uymalıdır:

1. **TEKNOLOJİ:** Rust 1.84+ (Tokio, Axum, Bollard, Pcap).
2. **YASAKLAR:** 
   - `unwrap()` ve `panic!` kullanımı KESİNLİKLE YASAKTIR. Tüm hatalar `Result` ile yönetilmelidir.
   - UI tarafında (`src/ui`) harici framework (React/Vue) kullanma. Saf ES6+ JS ve CSS Variables kuralımızdır.
3. **MİMARİ:** Hexagonal (Ports & Adapters) yapısı korunmalıdır. Core domain dış dünyayı (Docker/gRPC) bilmemelidir.
4. **GÜVENLİK:** mTLS sertifikaları yoksa sunucu ayağa kalkmamalıdır (Fail-Fast).
